# 監査 20260919_031706

- 対象: `tests/Core.Tests/BoardGeometryTests.cs`
- モデル: `gpt-5`
- トークン: 1957 / 8122

> 実装役（agy）とも分解役（Claude CLI）とも別のモデルによる指摘です。
> 内容は未検証です。採否は人間または分解役が判断してください。

---

以下の盲点があります。

- 何が: FreshBoard() が GameState 経由で Board を取得している
  なぜ問題か: 幾何の検査に初期化ロジック（RNG/初期配置/将来の仕様変更）が混入し、幾何不具合と初期化不具合が見分けにくくなる
  どう直すか: Board を直接生成できるファクトリ/コンストラクタを使うか、占有配列を明示初期化するテスト専用ヘルパに差し替える

- 何が: 乗算回り込みの中間値ケースが未検証（int.MaxValue と MinValue のみ）
  なぜ問題か: 実装が y*Width+x などの計算を境界判定前に行うと、最大値ではなく「閾値付近」で初めて負に回り込むバグを取り逃す
  どう直すか: 検証追加: y=(int.MaxValue/Board.Width)+1, x=1 などの「中間オーバーフロー閾値」を InBounds/IsOccupied に与え、例外なし・期待結果になることを Assert（または DoesNotThrow と組み合わせ）

- 何が: InBounds の境界網羅が点でのサンプリングのみ
  なぜ問題か: 実装の分岐（< と <= の取り違え等）が x/y の片側境界でのみ露呈するのを取り逃す
  どう直すか: リング状境界スイープを追加（x∈{-1,0,Board.Width-1,Board.Width}, y∈{-1,0,Board.Height-1,Board.Height} の直積すべてを検査）

- 何が: IsVisibleRow/IsBufferRow の「全域の正しさ」がサンプル値のみ
  なぜ問題か: 内部範囲 2..17 など未検査域の取り違え（見落とし規模でも）を取り逃す
  どう直すか: ループで y∈[0,VisibleHeight) は全て Visible、y∈[VisibleHeight,Height) は全て Buffer を Assert。対偶も合わせて全件確認

- 何が: EveryInternalRow_IsExactlyOneOfVisibleOrBuffer が 0 回ループでも緑になる可能性
  なぜ問題か: Height が誤って 0 のとき、実行なしでテストが成功扱いになる（「見ていないのに緑」）
  どう直すか: ループ前に Assert.Greater(Board.Height, 0)（もしくは Height==Visible+Buffer をここでも再主張）して不実行の緑化を防ぐ

- 何が: 初期盤面の空判定が点サンプルのみ
  なぜ問題か: 既定配置や配列初期化のバグが特定座標でのみ起きる場合に取り逃す
  どう直すか: FreshBoard の全セル走査で InBounds 内は全て IsOccupied==false を確認（Visible と Buffer 両方を含む）

- 何が: 「盤面外は常に占有」(y<0 含む) 仕様が RL-18 と完全一致するかの確認がない
  なぜ問題か: スポーン/回転アルゴリズムが y<0 を一時的に参照する設計だと衝突扱いになり得る（仕様-テスト乖離）
  どう直すか: RL-18 の原文で y<0 の扱いを確認。スポーン/回転が y<0 を読む経路がないことを E2E/プロパティテストで検証、あるいは y<0 だけは例外扱いする設計ならテストに反映

- 何が: 被検対象アセンブリの同一性（asmdef/多重定義）検証がない
  なぜ問題か: Unity/asmdef 構成変更でテストが別アセンブリ上の Board（ダミー/古い定義）を参照しても気づきにくい
  どう直すか: typeof(Board).Assembly.GetName().Name が想定 ("Game.Core" 等) であることを一度だけ Assert。CI で asmdef/GUID 漏れ検知に役立てる

```json
{"verdict":"concern","findings":["FreshBoardがGameState依存で幾何検査が初期化に汚染","中間値での乗算回り込み未検証（閾値テスト不足）","InBoundsの境界リング全組合せが未網羅","可視/バッファ行の全域判定がサンプルのみ","Height==0でループ未実行でも緑になる恐れ","初期盤面の空判定が点検のみで全走査なし","y<0も常に占有という仕様整合の確認不足","被検アセンブリ誤参照（asmdef）検知がない"]}