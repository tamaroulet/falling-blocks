# 監査 20260919_175858

- 対象: `tests/Core.Tests/MinoShapeSpawnTests.cs`
- モデル: `gpt-5`
- トークン: 2072 / 6822

> 実装役（agy）とも分解役（Claude CLI）とも別のモデルによる指摘です。
> 内容は未検証です。採否は人間または分解役が判断してください。

---

指摘1: Board 定数・関数をオラクルにしている
- 何が: InBounds/IsBufferRow/IsVisibleRow、Width/VisibleHeight/Height を期待値側に用いている。
- なぜ問題か: 実装の値にテスト期待値が従属すると、盤面寸法やバッファ行定義が誤っても「緑」のままになる（同語反復）。
- どう直すか: 仕様由来の独立定数（例: Width=10, VisibleHeight=20, Height=22, BufferRows=20..21）をテスト側に固定し、それに対して判定する。あるいは実装からではなく仕様モジュール（実装非依存）から読み込む。

指摘2: 出現基準位置（3,19）の検証が形だけ
- 何が: SpawnX/SpawnY= (3,19) をテスト内に固定し、その基準で「バッファ行に収まる」を検査しているだけ。実際のスポーン処理の原点を検証していない。
- なぜ問題か: 実装が別の原点でスポーンしても、MinoShape の相対セルが正しければテストは緑のまま（仕様とテストの乖離）。
- どう直すか: 実際のスポーナ（例: MinoSpawner.Spawn(...)）や Board の SpawnOrigin を公開させて検証する統合/結合テストを追加し、(3,19) での配置を直接確認する。少なくとも Board 側の SpawnOrigin 定数を 3/19 と一致検証するテストを別途用意。

指摘3: 型ごとの配列独立性検証が T のみ
- 何が: SpawnCells_ReturnsIndependentArrayEachCall は MinoType.T のみで配列の独立性を確認。
- なぜ問題か: 実装が T 以外では配列を使い回していても検出できない（経路未観測）。
- どう直すか: 全 MinoType を対象に「2 回呼び出しで別インスタンス」「内容は仕様どおり」を検証するパラメタライズドテストにする。

指摘4: AllTypes リストが列挙型と分離しておりドリフトの恐れ
- 何が: AllTypes を手書きで固定。MinoType に値が増減してもテストが追随せず未実行のまま緑になり得る。
- なぜ問題か: 「見ていないのに緑」の経路。未テストの新規値がスキップ扱いにもならない。
- どう直すか: Enum.GetValues(typeof(MinoType)) をソースにしてテスト生成する。あわせて「MinoType はちょうど 7 種」の仕様検証を別テストで追加。

指摘5: SpecCells が仕様表の単写で相関チェックが不足
- 何が: 形状表をそのまま書き写して集合同値のみを検証。
- なぜ問題か: 実装とテストが同じ誤写に汚染されると整合してしまう（張りぼて）。また副次的性質（対称性、外接 4x4 範囲、I/O の特殊オフセットなど）を見ていない。
- どう直すか: 独立した性質ベースの検査を追加（例: 各型の min/max X,Y、S/Z の鏡像関係、T の中心の位置、I/O の Y オフセットの差）。可能なら仕様ソースを別リポジトリ/生成物から読み込んで相互検証。

指摘6: セル順序に関する仕様の有無が未検証
- 何が: Is.EquivalentTo で順序を無視。順序に依存する処理（例: スキャン順、ピボットの先頭要素化）が将来発生した場合に検出できない。
- なぜ問題か: 実装が不安定順序でも緑になり、下流処理でバグを誘発。
- どう直すか: 「順序は仕様外である」ことを明記するか、順序が仕様で定まるなら SequenceEqual で厳密比較するテストを追加。

指摘7: 冗長かつ依存的な判定が混在
- 何が: Board.IsBufferRow/IsVisibleRow 判定と、同じ概念の範囲判定の二重チェック。いずれも Board 定義に依存。
- なぜ問題か: 依存先が誤っている場合、両方とも同じ誤りで緑化する。検査密度を上げているように見えるが独立性がない。
- どう直すか: 一方は仕様定数に基づく純粋な算術判定に切り替え、もう一方（関数呼び出し）は削るか、逆に Board 関数自体の単体テストで個別に検証。

指摘8: 境界・異常系の拡張不足（列挙・戻り値）
- 何が: 未定義列挙の例外は検証するが、戻り値の null/空配列/長さ≠4 などの異常は前半テストに委ねられ、原因特定性が低い。
- なぜ問題か: 失敗時にどこで壊れたか切り分けづらく、偽陽性/偽陰性を誘発。
- どう直すか: 冒頭で NotNull/Length==4 を個別に強制し、エラーメッセージで原因を特定可能にする。長さ超過や重複の個別検証も明示的に。

指摘9: ボード寸法仕様（10 幅/20 可視高）自体の検証がない
- 何が: 本テスト内では寸法仕様を実装値として受け入れているだけで、仕様値そのものの検証がない。
- なぜ問題か: ボード寸法が誤変更されても本テストは緑のまま（同語反復）。
- どう直すか: RL-01/RL-02/RL-15 の数値（10/20/22）を直接検証する専用テストを Core 側に追加し、MinoShape とは切り離して守る。

指摘10: スポーン原点の Y 軸方向の定義（上向き/下向き）の確認が不足
- 何が: 20..21 をバッファ行とみなす前提で書かれているが、Y 軸の正方向定義自体の検証がない。
- なぜ問題か: 軸定義の逆転（上が正）などの実装が入っても、Board 関数に依存していれば緑になり得る。
- どう直すか: 仕様起点の数直線定義（Y は下向きに増加）をテスト独立に検証するか、原点(0,0)付近のセルと可視領域の関係を数値で固定検証する。

```json
{"verdict": "concern", "findings": ["Boardの定数/関数を期待値に使用し同語反復", "スポーン原点(3,19)を実際の実装で検証していない", "配列独立性をT型のみで検証し全型未網羅", "AllTypesが手書きで列挙拡張時に未検出", "SpecCellsが単写で性質ベースの相関検証不足", "セル順序の仕様未確認で順序誤りを検出不可", "同義の判定をBoard依存で二重実施し独立性なし", "戻り値のNotNull/Length等の異常検証が弱い", "ボード寸法(10/20/22)の仕様検証が欠落", "Y軸正方向の仕様確認不足で逆転を検出不可"]}
```