# 監査 20260919_175711

- 対象: `tests/Core.Tests/FirstTickSpawnTests.cs`
- モデル: `gpt-5`
- トークン: 5608 / 11245

> 実装役（agy）とも分解役（Claude CLI）とも別のモデルによる指摘です。
> 内容は未検証です。採否は人間または分解役が判断してください。

---

指摘事項

- 何が: 盤面寸法と可視領域を Board.Width/Height/VisibleHeight に依存して検査している（CountBlocks、座標範囲検査、全域空の検査）。
  なぜ問題か: SUT の定数をそのまま期待値に使う同語反復。RL-01/RL-12 の誤値（例: 10×22/可視20でない）でも緑になる。
  どう直すか: テスト側に仕様値（Width=10, Height=22, VisibleHeight=20）を明示し、まず Board.* がそれと一致することを独立に検証。その後の走査や範囲検査も仕様値で行う。

- 何が: FirstTick_SpawnedMinoOccupiesOnlyBufferRows が MinoShape.SpawnCells（SUT）で期待セルを算出。
  なぜ問題か: 形状・出現オフセットの誤りがあっても同じ関数を使うため検出不能（張りぼてのオラクル）。
  どう直すか: テスト側で各ミノのスポーン形状（回転0のセル相対座標）を独立に定義した辞書を用いる。あるいは最低限、I/O/T/S/Z/J/L ごとに手計算のセルを直接チェックする。

- 何が: 7-Bag の再補充（2袋目以降）と袋境界の挙動が未検証。ヘッダに「7-Bag の補充」とあるが、初回袋のみ。
  なぜ問題か: RL-09 の実装不備（再補充忘れ、境界での RNG 消費数ズレ、オフバイワン）を見逃す。「見ていないのに緑」。
  どう直すか: 次のスポーンを強制できるテスト用フック（またはロックダウン/フォース進行のヘルパ）で7体消費して境界を跨ぎ、2袋目の順序と NextQueue=6 を検証。ReferenceBag を拡張して連続袋を計算し比較。

- 何が: Tick(巨大正数) の検査が無い（正の境界）。負値/0/1/2のみ。
  なぜ問題か: for ループで int→uint 回り込み、TickCount の加算オーバーフロー、時間爆発（int.MaxValue 回ループ）を見逃す（過去の失敗に合致）。
  どう直すか: 仕様を決めた上で、代表値（例: 10^6、int.MaxValue/2、int.MaxValue）で「まとめ進行」が安全に終わること、TickCount が期待通り（回り込まない/上限でクランプ/例外）を確認。実装が逐次ループなら防御（上限・早期打切り）を要求。

- 何が: InjectSeed_InPlaying_ChangesNothingObservable は現袋の Head+6 だけを比較。
  なぜ問題か: Playing 中の再シードが「次の袋」にだけ効いてしまうバグはここでは観測不能（境界未通過）。
  どう直すか: Playing 中に別シードを注入後、袋境界を跨いで2袋目以降の順序が元の連続 RNG と一致することを確認（注入が完全に無視される／適用フェーズが仕様通りであること）。

- 何が: 出現位置(X=3,Y=19)と向き(Spawn)を「出現した1種」に対してのみ検証。
  なぜ問題か: I/O など種別例外（横幅や中心の扱い）が混入しても、たまたま他種が出現すれば見逃す。
  どう直すか: 7種類すべてが先頭になるよう種別別のシードを用意し、各種で X=3/Y=19/Rotation=Spawn とバッファ内占有を検証。

- 何が: NextQueue_IsNotWritableFromOutside は IList かつ !IsReadOnly の場合のみ書換えを試みる。IsReadOnly の場合は何も起こさず緑。
  なぜ問題か: 機構的隔離の検証になっていない（慣習依存）。ReadOnly ラッパだが内部リスト共有などを見逃す可能性。
  どう直すか: 型検査で List<T> の直接返却でないことを確認し、IList にキャストしての書込で NotSupportedException が投げられることを明示的に検証。さらに連続取得で参照が異なる/スナップショットであることも確認。

- 何が: 仕様値（10×22、可視20）をメッセージ文言にだけ書き、実際の比較は SUT の定数に追随。
  なぜ問題か: 仕様とテストの乖離。誤った定数でも緑になる（同語反復）。
  どう直すか: 冒頭で Board.Width/Height/VisibleHeight が 10/22/20 と一致することを直値で assert。以降も直値を使用して境界検査を行う。

- 何が: 乱数未消費の検証（ReadyPhase_InputsDoNotConsumeRandomness）が最終順序一致のみ。
  なぜ問題か: 内部で一度消費→同じ状態に巻き戻す実装でも緑になり得る（「見ていないのに緑」）。
  どう直すか: RNG の消費回数が仕様で不変であることを観測可能にする（例: 公開しない内部カウンタのテストフック、あるいは連続袋比較で不整合が出ないことを境界跨ぎで確認）。

- 何が: InjectSeed の多重呼び（Ready 中に複数回、0→非0 の上書き等）の仕様未検証。
  なぜ問題か: 最終シード勝ち/初回のみ有効/0 の特別処理などの取り決めが間違っていても見逃し。
  どう直すか: Ready 中に複数回 InjectSeed を異なる値で呼び、最終的な出現順が仕様（例: 最後に注入された非0が有効、0は1に正規化）通りか検証。

```json
{"verdict": "concern", "findings": ["盤面寸法・可視高をSUT定数に依存し誤設定を検出不可", "MinoShapeを期待値に使用し形状/配置誤りを見逃す", "7-Bag再補充と袋境界の挙動が未検証（見出しと不整合）", "Tick(巨大正数)の回り込み/時間爆発を未検証", "Playing中の再シードが次袋へ影響しないこと未確認", "全ミノ種の出現位置/向きの網羅欠如（I/O例外の盲点）", "NextQueue不変性検証が弱く機構的隔離を保証せず", "仕様値(10×22,可視20)を直値で検査していない", "Ready中入力の乱数未消費を最終順序のみで判断", "InjectSeed多重呼びの仕様未検証"]}
```