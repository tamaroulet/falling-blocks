// <auto-generated>
// harness/testgen.py が単位定義 issue_17（sha256 ea05bcf52e6d）から出力した。手で編集しない。
// </auto-generated>

using Game.Core;
using NUnit.Framework;

namespace Game.Core.Tests.Generated
{
    public class Issue17Cases
    {
        [Test]
        [Description("ST-01")]
        public void Case_ready_first_tick_starts_playing()
        {
            var sut = new GameState();
            sut.Tick();
            Assert.That(sut.Phase, Is.EqualTo(GamePhase.Playing), "GameState.Phase");
        }

        [Test]
        [Description("RL-15")]
        public void Case_spawn_at_reference_position()
        {
            var sut = new GameState();
            sut.Tick();
            Assert.That(sut.ActiveMino?.X, Is.EqualTo(3), "GameState.ActiveMino.X");
            Assert.That(sut.ActiveMino?.Y, Is.EqualTo(19), "GameState.ActiveMino.Y");
            Assert.That(sut.ActiveMino?.Rotation, Is.EqualTo(Rotation.Spawn), "GameState.ActiveMino.Rotation");
        }

        [Test]
        [Description("LP-03")]
        public void Case_transition_tick_count_zero()
        {
            var sut = new GameState();
            sut.Tick();
            Assert.That(sut.TickCount, Is.EqualTo(0), "GameState.TickCount");
        }

        [Test]
        [Description("RL-24")]
        public void Case_no_natural_fall_before_interval()
        {
            var sut = new GameState();
            for (var step = 1; step <= 60; step++)
            {
                sut.Tick();
            }
            Assert.That(sut.ActiveMino?.X, Is.EqualTo(3), "GameState.ActiveMino.X");
            Assert.That(sut.ActiveMino?.Y, Is.EqualTo(19), "GameState.ActiveMino.Y");
            Assert.That(sut.ActiveMino?.Rotation, Is.EqualTo(Rotation.Spawn), "GameState.ActiveMino.Rotation");
        }

        [Test]
        [Description("RL-24")]
        public void Case_first_natural_fall_at_interval()
        {
            var sut = new GameState();
            for (var step = 1; step <= 61; step++)
            {
                sut.Tick();
            }
            Assert.That(sut.ActiveMino?.X, Is.EqualTo(3), "GameState.ActiveMino.X");
            Assert.That(sut.ActiveMino?.Y, Is.EqualTo(18), "GameState.ActiveMino.Y");
            Assert.That(sut.ActiveMino?.Rotation, Is.EqualTo(Rotation.Spawn), "GameState.ActiveMino.Rotation");
        }

        [Test]
        [Description("LP-04")]
        public void Case_tick_count_after_interval()
        {
            var sut = new GameState();
            for (var step = 1; step <= 61; step++)
            {
                sut.Tick();
            }
            Assert.That(sut.TickCount, Is.EqualTo(60), "GameState.TickCount");
            Assert.That(sut.Phase, Is.EqualTo(GamePhase.Playing), "GameState.Phase");
        }

        [Test]
        [Description("RL-36")]
        public void Case_conservation_holds_through_first_fall()
        {
            var sut = new GameState();
            for (var step = 1; step <= 61; step++)
            {
                sut.Tick();
            }
            Assert.That(sut.BlockCount, Is.EqualTo(0), "GameState.BlockCount");
            Assert.That(sut.LockedMinoCount, Is.EqualTo(0), "GameState.LockedMinoCount");
            Assert.That(sut.LinesCleared, Is.EqualTo(0), "GameState.LinesCleared");
            Assert.That(sut.Score, Is.EqualTo(0), "GameState.Score");
        }
    }
}
