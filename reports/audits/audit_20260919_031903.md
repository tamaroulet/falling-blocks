# 監査 20260919_031903

- 対象: `tests/Core.Tests/CoreContractTests.cs`
- モデル: `gpt-5`
- トークン: 1700 / 8043

> 実装役（agy）とも分解役（Claude CLI）とも別のモデルによる指摘です。
> 内容は未検証です。採否は人間または分解役が判断してください。

---

指摘:
- 何が: MinoType_HasSevenKindsInBagOrder が Enum.GetValues に依存して「宣言順」を検証している
  なぜ問題か: Enum.GetValues は基になる数値順に並ぶため、「宣言順」の検査になっていない。意図とテストの乖離
  どう直すか: 「宣言順」を要件化しないなら各メンバーの数値コードを明示検証に変更。宣言順を要件化するなら MetadataToken で並べて検証（仕様に未保証であることも併記）

- 何が: GamePhase_HasReadyPlayingGameOverInThatOrder が「順にのみ進む（ST-08）」を数値割当だけで代替している
  なぜ問題か: 数値が 0/1/2 でも遷移ロジックが逆走やスキップを許す実装は検出できない。「見ていないのに緑」
  どう直すか: 状態遷移 API/関数を駆動し、許可された遷移のみ成功・それ以外は失敗/例外になることを検証

- 何が: Rotation_HasFourOrientationsInClockwiseOrder が時計回り性を数値 0..3 で表すだけ
  なぜ問題か: 数値が 0..3 でも回転演算（CW/CCW、ラップアラウンド、負方向）が誤っていても検出不能。過去事例の符号や mod バグを拾えない
  どう直すか: 回転演算ユーティリティ（Next/Prev/Rotate(n)）を用意し、CW×4=Spawn、CCW×4=Spawn、CW/CCW 交互、n=±1,±5 等の境界を検証

- 何が: MinoType の「7-Bag 初期配列」を enum の並びで表現しようとしている
  なぜ問題か: 並びと意味が強結合だと、Unity で enum 値の変更や並び替えがアセットを壊す。GetValues 依存も不安定
  どう直すか: 「順序は基数で定義する」と仕様明記し、各メンバーの数値コードが [I=0,O=1,...,L=6] であることを個別に検証。Enum.GetNames で集合の固定も検証

- 何が: ActiveMino の「公開される型の形（IF-02）」がプロパティの存在確認だけで、形状（不変/書込可）が未検証
  なぜ問題か: 実装が set 可能に変わってもテストが気付かず、読み取り専用契約が破れる恐れ
  どう直すか: リフレクションで X/Y/Type/Rotation に public setter が無いこと、フィールドなら readonly であることを検証。型が struct の場合は readonly struct も検討

- 何が: ActiveMino_KeepsGivenCoordinatesVerbatim は入力の写経に留まり、算術の異常系（桁あふれ）を見ていない
  なぜ問題か: 後続処理で X/Y を加減算・乗算した際の回り込みや未検査変換の不具合を検出できない（既知の中間値回り込みリスク）
  どう直すか: 位置更新 API があるなら int.MaxValue/MinValue 近傍での増減、乗算・加算の経路で checked/long を用いたオーバーフロー検知をテスト。無いなら仕様で「算術は未定義/禁止」を明記

- 何が: Enum 集合の固定が不十分（GamePhase/Rotation は件数と個別値のみ）
  なぜ問題か: 新規メンバーの追加や別名の追加（同値異名）で集合が変わったときに意図せず動作が変わるか、失敗の理由が不明瞭
  どう直すか: Enum.GetNames() と期待名リストの完全一致を検証（順序は数値昇順か、仕様で定義した順）。同値異名の禁止も検証

- 何が: 右回転/左回転の「意味（幾何）」を数値だけで表現し、実空間との対応が未検証
  なぜ問題か: Right=1 でもブロックの変換（座標オフセットやマトリクス）が逆回転になっていても検出できない
  どう直すか: 各 Rotation に対する相対座標の変換結果（例: T ミノの4向き形状）をオラクルにし、Right/Left の幾何的意味を検証

- 何が: 「宣言順」や「あいまい語」が仕様コメントに残り、機械判定に変換されていない
  なぜ問題か: テストが仕様の誤写に忠実になりうる（仕様と実装の乖離）。保守時に解釈違いが起きる
  どう直すか: 仕様を「基数による順序定義」「許可遷移の表（隣接行列）」のように形式化し、テストもそれに追随

```json
{"verdict": "concern", "findings": ["Enum.GetValuesで宣言順を検証しており意図と不一致", "状態遷移の一方向性を数値で代替し挙動を未検証", "回転のCW/CCWやラップの挙動を未検証で境界バグを見逃す", "MinoTypeの順序と意味が強結合でUnity資産破壊リスク", "ActiveMinoの不変性・setter無を検証していない", "座標算術のオーバーフロー経路を検証していない", "Enum名集合の完全一致検証が不足し改変検知が弱い", "回転の幾何的意味（形状変換）を数値だけで判定している", "仕様の曖昧語・宣言順依存が機械判定に落ちていない"]}```