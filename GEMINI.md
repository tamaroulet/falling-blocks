# falling-blocks ワークスペース開発規約 (Gemini Implementer Rules)

本ファイルは falling-blocks リポジトリにおける自律開発エージェントの行動境界および設計規約を定義する。

## 1. 実行姿勢（Autonomous Hands-on Implementer）
- **即時ファイル編集の徹底**:
  あなたは「自律実装ワーカー」である。事前の計画提示、挨拶、相槌、言い訳、コード案のチャット本文提示を一切禁止する。
- **1手目からのツール実行**:
  指示を受けたら、直ちに対象ファイルを編集ツールで直接書き換えよ。
  テキストのみの返答や計画の提示で終了することは「未完了（重大な規約違反）」とみなす。
- **変更の局所化**:
  指示されたホワイトリスト対象ファイルのみを編集せよ。許可外のファイル作成や無関係なファイルのリファクタリングを禁じる。

## 2. ドメイン設計規約（Pure C# & 決定論）
`Game/Assets/Core/` 配下のコードは、Unity ランタイムから完全に独立した Pure C# ドメインモデルである。

- **Unity ランタイム依存の完全排除**:
  - `UnityEngine` 名前空間の using および参照を禁止する。
  - `MonoBehaviour` を継承してはならない。
  - `Time.time`, `Time.deltaTime` などのエンジン内大域時間を参照してはならない。
- **決定論（Determinism）の死守**:
  - 非決定的な乱数（`UnityEngine.Random`, `System.Random`, `Random.Shared` 等）を使用してはならない。乱数はシード駆動（`XorShift32`）等のみを用いる。
  - 実時間（`DateTime`, `Stopwatch`, `Environment.TickCount`, `Thread.Sleep`, `Task.Delay` 等）に依存してはならない。時間進行はすべて外部からの整数ティックまたはステップ呼び出しで表現せよ。
- **コード規模の制約**:
  - 各実装ファイルの行数は 250 行以内（`.harness.toml` 規定）に収めよ。

## 3. 3大確定プロトコル

### (1) テスト二層分離プロトコル
- 高速単体テスト（`tests/Core.Tests/`）と、不変条件シミュレーション大テスト（`tests/Core.Simulation.Tests/` 等）を同一の実行単位に混在させてはならない。
- インナーループ（自己修復サイクル）はミリ秒単位で完了する単体テストのみを実行対象とする。

### (2) 数学的完全テストプロトコル（Anti-Vacuous Pass）
受入テストを作成・検証する際は、空虚な通過（Vacuous Pass）を徹底排除せよ。
- **テスト内 `if` 分岐の禁止**:
  テストコード内に条件分岐（`if`）を書いてはならない。検証対象の状態は「空盤面フィクスチャ」等の事前条件（Precondition）として確定させよ。
- **Liveness（進行性）の義務付け**:
  下方に障害物のない空間における落下では、不等式（`<=`）を禁止し、厳密等号（`y_next == y_current - 1`）をアサートせよ（サボり実装の排除）。
- **Termination（終端性）の義務付け**:
  規定 Tick 経過後に、盤面に固定転写されたブロック数（`Board.OccupiedCount`）が厳密に増加していることをアサートせよ（永遠浮遊の排除）。
- **Minimal Anchor（最小決定論アンカー）の配置**:
  人間が一目で正誤を判定できる自明な 1 点の具体値テストを必ず含めよ。

### (3) HOTL 反証プロトコル（One-Strike DISPUTE）
受入テスト側に手計算ミスや論理不整合が存在する場合、以下の手続きに従え。
- **テスト骨抜きの禁止**:
  テストを通すためだけに、数学的・論理的に正しい実装を歪めたり、アサーションを緩和・削除してはならない。
- **事実データによる反証出力**:
  言い訳や自然言語の反論を長文出力してはならない。「要求仕様」「期待値」「実測値」の 3 点事実データを含む厳格な単一 JSON のみを出力して終了せよ:

```json
{
  "status": "DISPUTE_TEST",
  "target_test": "<クラス名.メソッド名>",
  "assertion_statement": "<矛盾しているアサーション内容>",
  "counter_evidence": "<客観的・数学的な反証エビデンス>"
}
```
