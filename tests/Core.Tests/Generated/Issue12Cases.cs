// <auto-generated>
// harness/testgen.py が単位定義 issue_12（sha256 524f503367fd）から出力した。手で編集しない。
// </auto-generated>

using Game.Core;
using NUnit.Framework;

namespace Game.Core.Tests.Generated
{
    public class Issue12Cases
    {
        [Test]
        [Description("ST-01")]
        public void Case_ready_initial()
        {
            var sut = new GameState();
            Assert.That(sut.Phase, Is.EqualTo(GamePhase.Ready), "GameState.Phase");
            Assert.That(sut.ActiveMino, Is.Null, "GameState.ActiveMino");
            Assert.That(sut.NextQueue, Is.Empty, "GameState.NextQueue");
            Assert.That(sut.Score, Is.EqualTo(0), "GameState.Score");
            Assert.That(sut.LinesCleared, Is.EqualTo(0), "GameState.LinesCleared");
            Assert.That(sut.TickCount, Is.EqualTo(0), "GameState.TickCount");
        }

        [Test]
        [Description("ST-06")]
        public void Case_seed_zero_keeps_state()
        {
            var sut = new GameState();
            var before0 = sut.Phase;
            var before1 = sut.ActiveMino;
            var before2 = sut.NextQueue;
            var before3 = sut.Score;
            var before4 = sut.LinesCleared;
            var before5 = sut.TickCount;
            sut.InjectSeed(0u);
            Assert.That(sut.Phase, Is.EqualTo(before0), "GameState.Phase");
            Assert.That(sut.ActiveMino, Is.EqualTo(before1), "GameState.ActiveMino");
            Assert.That(sut.NextQueue, Is.EqualTo(before2), "GameState.NextQueue");
            Assert.That(sut.Score, Is.EqualTo(before3), "GameState.Score");
            Assert.That(sut.LinesCleared, Is.EqualTo(before4), "GameState.LinesCleared");
            Assert.That(sut.TickCount, Is.EqualTo(before5), "GameState.TickCount");
        }

        [Test]
        [Description("ST-06")]
        public void Case_seed_nonzero_keeps_state()
        {
            var sut = new GameState();
            var before0 = sut.Phase;
            var before1 = sut.ActiveMino;
            var before2 = sut.NextQueue;
            var before3 = sut.Score;
            var before4 = sut.LinesCleared;
            var before5 = sut.TickCount;
            sut.InjectSeed(12345u);
            Assert.That(sut.Phase, Is.EqualTo(before0), "GameState.Phase");
            Assert.That(sut.ActiveMino, Is.EqualTo(before1), "GameState.ActiveMino");
            Assert.That(sut.NextQueue, Is.EqualTo(before2), "GameState.NextQueue");
            Assert.That(sut.Score, Is.EqualTo(before3), "GameState.Score");
            Assert.That(sut.LinesCleared, Is.EqualTo(before4), "GameState.LinesCleared");
            Assert.That(sut.TickCount, Is.EqualTo(before5), "GameState.TickCount");
        }

        [Test]
        [Description("RL-01")]
        public void Case_visible_row_bottom()
        {
            var first = Board.IsVisibleRow(0);
            var second = Board.IsVisibleRow(0);
            Assert.That(first, Is.EqualTo(true), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsVisibleRow を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-01")]
        public void Case_visible_row_top()
        {
            var first = Board.IsVisibleRow(19);
            var second = Board.IsVisibleRow(19);
            Assert.That(first, Is.EqualTo(true), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsVisibleRow を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-01")]
        public void Case_visible_row_below()
        {
            var first = Board.IsVisibleRow(-1);
            var second = Board.IsVisibleRow(-1);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsVisibleRow を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-01")]
        public void Case_visible_row_buffer()
        {
            var first = Board.IsVisibleRow(20);
            var second = Board.IsVisibleRow(20);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsVisibleRow を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-02")]
        public void Case_buffer_row_top_visible()
        {
            var first = Board.IsBufferRow(19);
            var second = Board.IsBufferRow(19);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsBufferRow を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-02")]
        public void Case_buffer_row_first()
        {
            var first = Board.IsBufferRow(20);
            var second = Board.IsBufferRow(20);
            Assert.That(first, Is.EqualTo(true), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsBufferRow を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-02")]
        public void Case_buffer_row_last()
        {
            var first = Board.IsBufferRow(21);
            var second = Board.IsBufferRow(21);
            Assert.That(first, Is.EqualTo(true), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsBufferRow を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-02")]
        public void Case_buffer_row_above()
        {
            var first = Board.IsBufferRow(22);
            var second = Board.IsBufferRow(22);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsBufferRow を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-03")]
        public void Case_in_bounds_origin()
        {
            var first = Board.InBounds(0, 0);
            var second = Board.InBounds(0, 0);
            Assert.That(first, Is.EqualTo(true), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.InBounds を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-03")]
        public void Case_in_bounds_far_corner()
        {
            var first = Board.InBounds(9, 21);
            var second = Board.InBounds(9, 21);
            Assert.That(first, Is.EqualTo(true), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.InBounds を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-03")]
        public void Case_in_bounds_right()
        {
            var first = Board.InBounds(10, 0);
            var second = Board.InBounds(10, 0);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.InBounds を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-03")]
        public void Case_in_bounds_left()
        {
            var first = Board.InBounds(-1, 0);
            var second = Board.InBounds(-1, 0);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.InBounds を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-03")]
        public void Case_in_bounds_above()
        {
            var first = Board.InBounds(0, 22);
            var second = Board.InBounds(0, 22);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.InBounds を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-03")]
        public void Case_in_bounds_extremes()
        {
            var first = Board.InBounds(-2147483648, 2147483647);
            var second = Board.InBounds(-2147483648, 2147483647);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.InBounds を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-04")]
        public void Case_occupied_empty_origin()
        {
            var sut = new Board();
            var first = sut.IsOccupied(0, 0);
            var second = sut.IsOccupied(0, 0);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsOccupied を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-04")]
        public void Case_occupied_empty_far_corner()
        {
            var sut = new Board();
            var first = sut.IsOccupied(9, 21);
            var second = sut.IsOccupied(9, 21);
            Assert.That(first, Is.EqualTo(false), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsOccupied を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-18")]
        public void Case_occupied_outside_left()
        {
            var sut = new Board();
            var first = sut.IsOccupied(-1, 0);
            var second = sut.IsOccupied(-1, 0);
            Assert.That(first, Is.EqualTo(true), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsOccupied を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-18")]
        public void Case_occupied_outside_above()
        {
            var sut = new Board();
            var first = sut.IsOccupied(0, 22);
            var second = sut.IsOccupied(0, 22);
            Assert.That(first, Is.EqualTo(true), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsOccupied を 2 回呼んで戻り値が変わった");
        }

        [Test]
        [Description("RL-18")]
        public void Case_occupied_outside_extremes()
        {
            var sut = new Board();
            var first = sut.IsOccupied(-2147483648, -2147483648);
            var second = sut.IsOccupied(-2147483648, -2147483648);
            Assert.That(first, Is.EqualTo(true), "return");
            Assert.That(second, Is.EqualTo(first), "IDEMPOTENT: Board.IsOccupied を 2 回呼んで戻り値が変わった");
        }
    }
}
